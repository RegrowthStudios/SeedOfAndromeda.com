// Initiate Dev News Slider
$(document).ready(function () {
    var elems = $("#dev-news .media-wrapper");
    var sliderFrame = $("#dev-news.media-slider-frame");
    var slider = new MediaSlider(elems, sliderFrame);
});
